data
======

Purpose
- Runtime data storage for recordings and other generated artifacts.

Contents
- `recordings/` — where audio WAVs created by the recorder are written by default.

Usage
- `main.py` creates `data/recordings` at startup and writes session audio files there.
