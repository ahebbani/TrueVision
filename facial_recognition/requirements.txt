numpy==2.3.3
# Core app-side packages. On Raspberry Pi, install OpenCV and dlib via apt per SETUP_PI.md.
Pillow
luma.oled
sounddevice
soundfile

# Optional speech-to-text (large install on ARM):
# faster-whisper  # install from requirements-faster-whisper.txt if desired