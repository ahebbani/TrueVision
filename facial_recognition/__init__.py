"""Face recognition models & helper scripts.

Entry point moved to top-level `main.py`. This package now only contains:
- `add_face.py`, `manage_embeddings.py`, `visualize_db.py`, and `models/`

Import the app via `import main` for the runtime.
"""

__all__: list[str] = []
