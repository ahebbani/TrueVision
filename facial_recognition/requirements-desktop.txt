# Extra dependencies for desktop development (x86_64/macOS/Windows)
# Use ONLY if you are not on Raspberry Pi. On the Pi, prefer apt packages.

opencv-python
# On some platforms you may need opencv-python-headless instead

dlib
# If this fails to build from source, install dlib via your OS package manager

# Optional speech-to-text (big):
faster-whisper
